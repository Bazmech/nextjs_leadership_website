# Productive Leadership Studio

Standalone Sanity Studio for project `vrub9uq4` (dataset `production`). Project ID and dataset are set in `sanity.config.js` — no `.env` file is required.

```bash
npm install
npm run dev
```

Opens at http://localhost:3333.

```bash
npx sanity login
npm run deploy-schema
npm run deploy
```
